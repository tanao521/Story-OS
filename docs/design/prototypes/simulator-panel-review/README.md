# Simulator Panel Review high-fidelity prototype

Open `index.html` through a local static server from the repository root (for example, a temporary Python `http.server`) so fixture `fetch()` calls work. The prototype is isolated under `docs/design`, is not referenced by `web/templates/index.html`, and has no production API calls or writes.

The “设计预览” selector switches all required states. The signature is the vertical **evidence rail**: Story OS gold marks authoritative reading, violet marks model supplement, and a ruled audit column keeps selection, evidence, usage, and staleness visible without a dashboard-card collage.
