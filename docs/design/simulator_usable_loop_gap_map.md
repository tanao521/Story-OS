# Simulator Usable Loop Gap Map

```text
visible simulator entry
  -> context navigator [implemented]
  -> narrative-turn scope [partial: branch/view not carried into first entry]
  -> plan / feasibility / preview [implemented and visible]
  -> confirm [API + control, but no next-turn UI]
  -> Turn History [internal only]
  -> compile Candidate [API only]
  -> review / approve [missing]
  -> ChapterCommitService commit [API only]
  -> next chapter [not connected to simulator]
```

The gap is product wiring and evidence, not permission to rewrite the sealed 0D4 authority or commit services.
