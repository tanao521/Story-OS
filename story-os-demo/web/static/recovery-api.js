(() => {
  const request = async (path, options = {}) => { const r = await fetch(path, options); const b = await r.json(); if (!b.ok) throw new Error((b.errors || [b.message])[0]); return b.result; };
  let activeProject = null;
  window.storyosApi = { request, projects: () => request('/api/projects'), jobs: () => request('/api/jobs'), planning: (kind) => request(`/api/planning/${kind}`), revisions: () => request('/api/revisions'), archive: () => request('/api/archive'), createJob: (job_type, parameters = {}) => request('/api/jobs', {method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({job_type,parameters})}), createRevision: (chapter_id, reason='') => request('/api/revisions',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({chapter_id,reason})}), activateProject: async (id) => { const result=await request(`/api/projects/${id}/activate`,{method:'POST'}); activeProject=id; window.dispatchEvent(new CustomEvent('storyos:project-changed',{detail:{projectId:id}})); window.dispatchEvent(new Event('storyos:refresh-operations')); window.dispatchEvent(new Event('storyos:refresh-narrative-memory')); return result; }, clearProjectState: () => { activeProject=null; window.dispatchEvent(new CustomEvent('storyos:project-cleared')); }, activeProject: () => activeProject };
})();
